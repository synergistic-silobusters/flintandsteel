/* global angular */

angular.module('flintAndSteel')
.controller('LearnCtrl',
    [
        'paginateSvc',
        function(paginateSvc) {
            "use strict";

            var ctrl = this;
            

        }
    ]
);
